# Gateway runtime — set on Railway gateway service (Variables tab).
# Do NOT set PORT here; Railway injects it automatically.
BACKEND_URL=https://api.katariastoneworld.com

# Networking → Public domain → Target port must match the PORT shown in deploy logs.
# Use "Auto" or the same number Railway prints as [gateway] PORT=...
