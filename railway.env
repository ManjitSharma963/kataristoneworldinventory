# Gateway runtime variables — Railway can import this from the Variables tab (RAW editor).
# These are also baked into deploy/gateway/Dockerfile as defaults on deploy.

BACKEND_URL=https://api.katariastoneworld.com

# Optional: use Railway private networking instead (replace "api" with your backend service name):
# BACKEND_URL=http://api.railway.internal:8080
